version: 2
jobs:
    build:
        machine: true
        working_directory: /go/src/github.com/byuoitav
        steps:
            - checkout: go get github.com/byuoitav/pi-credentials-microservice
            - run: cd pi-credentials-microservice 
            - run: make docker
