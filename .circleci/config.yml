version: 2
jobs:
    build:
        working_directory: /go/src/github.com/byuoitav/raspi-credentials-microservice
        steps:
            - checkout
            - run:
                command: make docker
                  

